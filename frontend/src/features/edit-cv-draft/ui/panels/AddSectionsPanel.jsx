import { getSectionDefinition } from '@/entities/cv'

export default function AddSectionsPanel({ availableKeys, sections, onAdd, onLocate }) {
  return <div className="space-y-6"><section><h3 className="font-bold">Mục chưa sử dụng</h3><p className="mb-3 text-xs text-slate-500">Kéo và thả mục bất kỳ vào vị trí bạn muốn thêm trên CV</p><div className="grid grid-cols-2 gap-2">{availableKeys.map((key) => <button key={key} type="button" onClick={() => onAdd(key)} className="rounded-lg border border-dashed border-emerald-300 bg-emerald-50 p-2 text-left text-sm font-semibold text-emerald-800">+ {getSectionDefinition(key)?.displayName || key}</button>)}</div></section><section><h3 className="font-bold">Mục đã sử dụng</h3><p className="mb-3 text-xs text-slate-500">Click để xem vị trí của mục trên CV</p><div className="space-y-2">{sections.map(({ section }) => <button key={section.instance_id} type="button" onClick={() => onLocate(section.instance_id)} className="w-full rounded-lg border border-slate-200 bg-white p-2 text-left text-sm">{section.title}</button>)}</div></section></div>
}
