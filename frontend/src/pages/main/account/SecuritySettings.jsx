import { SessionManager } from '@/features/session-management'

// Trang "Cài đặt bảo mật" (candidate): quản lý thiết bị đang đăng nhập.
export default function SecuritySettings() {
  return <SessionManager />
}
