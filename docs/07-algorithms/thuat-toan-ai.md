# 07 - Algorithms (AI)

Phạm vi (theo PRD mục 7):
- Đọc CV PDF (PyMuPDF) và trích xuất kỹ năng theo `skills` (keyword + alias matching)
- Train model phân loại nhóm kỹ năng (TF-IDF + LogisticRegression/LinearSVC, scikit-learn), lưu `.pkl`
- Công thức tính điểm phù hợp CV - Job (match_score, xem `job_matcher.py`)
- Tiêu chí chấm điểm câu trả lời phỏng vấn (rule-based, thang 0-10)
- Tiêu chí đánh giá mô hình (accuracy, precision/recall/F1, confusion matrix, ngưỡng tối thiểu 75-80%)

Chưa triển khai — thuộc Giai đoạn 2 (AI cơ bản) theo lộ trình DB mục 7.
